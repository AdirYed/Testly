module.exports = {
    prefix: "tw-",
    theme: {
        extend: {
            colors: {
                primary: "var(--primary-color)",
                secondary: "var(--secondary-color)"
            },

            spacing: {
                "80": "20rem"
            }
        }
    },
    variants: {},
    plugins: []
};
